[Unit]
Description=nexus service
After=network.target

[Service]
Type=forking
LimitNOFILE=65536
ExecStart=/opt/nexus-{{ nexus.full_version }}/bin/nexus start
ExecStop=/opt/nexus-{{ nexus.full_version }}/bin/nexus stop

User=nexus
Restart=on-abort
TimeoutSec=600

[Install]
WantedBy=multi-user.target